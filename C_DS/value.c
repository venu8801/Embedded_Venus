#include<stdio.h>
void main()
{
	int opt,num,c,i,rem,sum=0,bit,x,z,o;
	printf("1.101 repeated,2.count no.of0's,3.binary value,4.swap the bits,5.101 to 111");
	printf("select the option");
	scanf("%d",&opt);
	printf("enter the value");
	scanf("%d",&x);
	switch(opt)
	{
		case 1:

		
